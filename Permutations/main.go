package main

func main() {

	// for str := range GeneratePermutation(alphabet) {
	// 	fmt.Println(str)
	// }

	// num, err := strconv.Atoi(os.Args[1])
	// if err != nil {
	// 	fmt.Println(err.Error())
	// 	os.Exit(1)
	// }

	// noOfPerms := permutation1(rangeSlice(0, num))
	// fmt.Println("Number of Permutation:", len(noOfPerms))
	// for i := 0; i < len(noOfPerms); i++ {
	// 	fmt.Println(noOfPerms[i])
	// }

	// d := permutation2("123456789")
	// fmt.Print(d)
}
